import {MetricSizes} from '@/Themes';
import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  parentContainer: {flex: 1},
  mainContainer: {flex: 1, marginHorizontal: MetricSizes.hs20},
});
